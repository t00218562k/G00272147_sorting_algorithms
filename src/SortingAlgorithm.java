/**
 * Interface defining a sorting algorithm.
 * Classes that implement this interface need to provide
 * a method to sort an integer array.
 */
public interface SortingAlgorithm {
    void sort(int[] array);
}
